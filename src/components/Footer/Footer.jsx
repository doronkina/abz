import React from 'react'

import s from './Footer.module.scss'

const Footer = () => {
    return (
        <footer className={s.wrapper}>
            <p className={s.container}>© abz.agency specially for the test task</p>
        </footer>
    )
}

export default Footer